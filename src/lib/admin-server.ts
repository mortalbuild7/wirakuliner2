/**
 * @deprecated Import dari `@/lib/admin-auth` — file ini hanya re-export kompatibilitas.
 */
export {
  requireAdmin,
  requireSuperAdmin,
  assertSuperAdminPage,
  SUPER_ADMIN_DB_ROLE,
  type AdminSession,
} from "@/lib/admin-auth";
